console.log('T3', process.env.API_URL);

//const config = useRuntimeConfig();
//import { toRaw } from 'vue';

//import { useRuntimeConfig } from '#imports';

/*
import { useRuntimeConfig } from '#app';

export const useA = () => {
  const config = useRuntimeConfig();
  const nuxtApp = useNuxtApp();
  
  console.log('useA', config.apiUrl, config.API_URL, process.env.NUXT_PUBLIC_API_URL);

  console.log('NUXT', nuxtApp.$config.public.apiUrl, nuxtApp.$config.public);
  return process.env.API_URL;
};

export default () => {
  //const config = useRuntimeConfig();
  //console.log(config);
}; */
